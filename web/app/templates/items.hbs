{{partial 'header'}}
<div class="main container-fluid">
    <div class="row">
    {{#for 1 3}}
        <div id="column{{this}}" class="column col-md-4">
        {{#each controller}}
            {{partial 'picture'}}
        {{/each}}
        </div>
    {{/for}}
    </div>
</div>